version: 2.0
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run:
          name: Build jar
          command: gradle jar
      - run:
          name: Gradle test
          command: gradle test
      - store_test_results:
          path: build/test-results
      - store_artifacts:
          path: build/libs
